package PaooGame.Tiles;

import PaooGame.Graphics.Assets;

public class FirstModelHouseTile {
    public static class FirstModelHouseTileOne extends Tile
    {
        /*! \fn public GrassTile(int id)
            \brief Constructorul de initializare al clasei

            \param id Id-ul dalei util in desenarea hartii.
         */
        public FirstModelHouseTileOne(int id)
        {
            /// Apel al constructorului clasei de baza
            super(Assets.FirstModelHouseOne, id);
        }
    }
    public static class FirstModelHouseTileTwo extends Tile
    {
        /*! \fn public GrassTile(int id)
            \brief Constructorul de initializare al clasei

            \param id Id-ul dalei util in desenarea hartii.
         */
        public FirstModelHouseTileTwo(int id)
        {
            /// Apel al constructorului clasei de baza
            super(Assets.FirstModelHouseTwo, id);
        }
    }
    public static class FirstModelHouseTileThree extends Tile
    {
        /*! \fn public GrassTile(int id)
            \brief Constructorul de initializare al clasei

            \param id Id-ul dalei util in desenarea hartii.
         */
        public FirstModelHouseTileThree(int id)
        {
            /// Apel al constructorului clasei de baza
            super(Assets.FirstModelHouseThree, id);
        }
    }
    public static class FirstModelHouseTileFour extends Tile
    {
        /*! \fn public GrassTile(int id)
            \brief Constructorul de initializare al clasei

            \param id Id-ul dalei util in desenarea hartii.
         */
        public FirstModelHouseTileFour(int id)
        {
            /// Apel al constructorului clasei de baza
            super(Assets.FirstModelHouseFour, id);
        }
    }
    public static class FirstModelHouseTileFive extends Tile
    {
        /*! \fn public GrassTile(int id)
            \brief Constructorul de initializare al clasei

            \param id Id-ul dalei util in desenarea hartii.
         */
        public FirstModelHouseTileFive(int id)
        {
            /// Apel al constructorului clasei de baza
            super(Assets.FirstModelHouseFive, id);
        }
    }
    public static class FirstModelHouseTileSix extends Tile
    {
        /*! \fn public GrassTile(int id)
            \brief Constructorul de initializare al clasei

            \param id Id-ul dalei util in desenarea hartii.
         */
        public FirstModelHouseTileSix(int id)
        {
            /// Apel al constructorului clasei de baza
            super(Assets.FirstModelHouseSix, id);
        }
    }
}
